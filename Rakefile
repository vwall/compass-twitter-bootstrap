#!/usr/bin/env rake
require "bundler/gem_tasks"

desc 'Convert less to scss'
task :convert do
  ruby('build/convert.rb')
end
desc 'Upgrade to the latest version of Twitter Bootstrap'
task :upgrade do
  ruby('build/upgrade.rb')
end
